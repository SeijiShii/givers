---
import BaseLayout from '../../layouts/BaseLayout.astro';
import AdminMessageThreadList from '../../components/react/AdminMessageThreadList';
import { t } from '../../lib/i18n';

const locale = 'ja';
---

<BaseLayout title={t(locale, 'messages.manage')} locale={locale}>
  <AdminMessageThreadList
    client:load
    locale={locale}
  />
</BaseLayout>
